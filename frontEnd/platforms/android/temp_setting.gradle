android {
            defaultConfig {
                applicationId = "org.nativescript.cargoDeliverTest"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }
android {
            defaultConfig {
                applicationId = "org.nativescript.cargoExpress"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }
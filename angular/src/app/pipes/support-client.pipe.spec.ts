import { SupportClientPipe } from "./support-client.pipe";

describe("SupportClientPipe", () => {
  it("create an instance", () => {
    const pipe = new SupportClientPipe();
    expect(pipe).toBeTruthy();
  });
});

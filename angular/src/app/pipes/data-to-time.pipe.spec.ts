import { DataToTimePipe } from "./data-to-time.pipe";

describe("DataToTimePipe", () => {
  it("create an instance", () => {
    const pipe = new DataToTimePipe();
    expect(pipe).toBeTruthy();
  });
});
